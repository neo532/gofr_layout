# CLAUDE.md

> 本仓库的全 agent 通用规则统一写在 `AGENTS.md`。Claude Code 自动加载本文件，请一并读取 `AGENTS.md`。

- 硬性规范 / 风格约定 / 模块布局 / 验证流程：见 `AGENTS.md`。
- 分层规则：`docs/rules/`；Spec 变更集：`docs/specs/`；完成条件：`make fitness` / `make loop`。
